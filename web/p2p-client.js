// Placeholder browser client bundle. The real file is produced by the
// TypeScript build: cd clients/ts && npm run build, which copies
// dist/browser/p2p-client.js here before the Go binary is built.
export const P2PGame = undefined;
console.warn("p2p-client.js placeholder: build clients/ts to get the real browser bundle");
